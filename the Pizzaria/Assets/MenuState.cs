using UnityEngine;
using System.Collections;

public class MenuState : MonoBehaviour {
    static private int BaseSelected { get; set; }
    static private bool cheese { get; set; }
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
